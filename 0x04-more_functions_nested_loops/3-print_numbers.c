#include "main.h"

/**
 * print_numbers - check if char is digit
 * Return: 1 if char is digit, otherwise 0.
 */
void print_numbers(void)
{
	int n;

	for (n = 0; n < 10; n++)
		_putchar('0' + n);
	_putchar("\n");
	return (0);
}
