#include "main.h"


/**
 * mul - check if char is digit
 * @a: is the char to be checked
 * @b: is the char to be checked
 * Return: 1 if char is digit, otherwise 0.
 */
int mul(int a, int b)
{
	return (a * b);

}
